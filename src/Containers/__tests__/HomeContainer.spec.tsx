describe('HomeContainer', () => {
  it('WHEN start screen SHOULD render correctly', () => {
    // should search bar to be visible
  })

  it('WHEN search button is pressed SHOULD call api AND SHOULD show results', () => {
    // should search bar to be pressed
    // should show items if api response with data
    // should show drink name, total of ingredients and image
  })

  it('WHEN api is Loading SHOULD show loading message', () => {
    // should show loading message WHEN isLoading response is true
  })

  it('WHEN there is no response from api SHOULD show empty message', () => {
    // should show empty message WHEN data response is empty
  })

  it('WHEN api response with error SHOULD show error message', () => {
    // should show error message WHEN error response is true
  })

  it('WHEN cocktail is pressed SHOULD navigate to drink details', () => {
    // should navigate to correct screen, passing correct params
  })
})
